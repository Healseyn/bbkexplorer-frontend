# Backend API base URL
# Include /api because the client calls routes like /blocks, /stats, etc.
# For local development, use: http://localhost:3001/api
# For production, set this to your production API URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api
